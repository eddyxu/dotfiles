 Git Configuration
----------------------------

1. Firstly move git/gitignore to ~/.gitignore
2. Then run
  $ git config --global user.name "YOUR NAME"
  $ git config --global user.email "yourname@email.com"
